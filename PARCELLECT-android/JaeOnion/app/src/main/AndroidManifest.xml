<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jae_onion.sungminkim.jaeonion">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="PARCELLECT"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity android:name=".AgreeActivity"
            android:theme="@style/AppTheme"></activity>
        <activity android:name=".JoinActivity"
            android:theme="@style/AppTheme"></activity>
        <activity android:name=".MainActivity"
            android:theme="@style/AppTheme"></activity>
        <activity android:name=".MypageActivity"
            android:theme="@style/AppTheme">
        </activity>

        <activity android:name=".ShoppingActivity"
            android:theme="@style/AppTheme"></activity>

        <activity android:name=".ProductReceiveActivity"
            android:theme="@style/AppTheme"></activity>

        <activity android:name=".ProductSendActivity"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>