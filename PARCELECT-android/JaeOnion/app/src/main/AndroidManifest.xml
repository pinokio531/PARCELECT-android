<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jae_onion.sungminkim.jaeonion">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="PARCELECT"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".InitialActivity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity android:name=".InitialActivity.AgreeActivity"
            android:theme="@style/AppTheme"></activity>
        <activity android:name=".InitialActivity.JoinActivity"
            android:theme="@style/AppTheme"></activity>
        <activity android:name=".MainPage.MainActivity.MainActivity"
            android:theme="@style/AppTheme"></activity>
        <activity android:name=".MainPage.DrawerItem.MypageActivity"
            android:theme="@style/AppTheme">
        </activity>

        <activity android:name=".MainPage.DrawerItem.Shopping.ShoppingActivity"
            android:theme="@style/AppTheme"></activity>

        <activity android:name=".MainPage.DrawerItem.ProductReceiveActivity"
            android:theme="@style/AppTheme"></activity>

        <activity android:name=".MainPage.MainActivity.GridViewAdapter.MoreCompanyActivity"
            android:theme="@style/AppTheme"></activity>

        <activity android:name=".MainPage.DrawerItem.SendProduct.ProductSendFragmentActivity"
            android:theme="@style/AppTheme"></activity>

    </application>

</manifest>